import { PersoDirectiveDirective } from './perso-directive.directive';

describe('PersoDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PersoDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});

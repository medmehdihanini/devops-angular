import { DeleteTableDirectiveDirective } from './delete-table-directive.directive';

describe('DeleteTableDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteTableDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
